# MEMORY.md — Long-Term Memory

*Marcus maintains this file. It holds durable facts about the user that should survive across sessions.*

*Format: each entry begins with a date heading (### YYYY-MM-DD) followed by the note.*

*(Empty on first use — memories accumulate here over time.)*
